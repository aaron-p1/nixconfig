import "plugins.luasnip.snippets.utils" as {
  :s
  :i
  :rep
  :fmta

  :readTemplateFile
  :oFileStart
}

export default {
  s(
    "initmod",
    fmta(
      readTemplateFile("nix-mod.nix"), {
      rep(1),
      i(1),
      i(2),
      i(0)
    }),
    oFileStart
  )
}
