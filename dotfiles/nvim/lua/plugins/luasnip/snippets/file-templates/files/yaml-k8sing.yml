apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: <>
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: <>
        pathType: <>
        backend:
          service:
            name: <>
            port:
              number: <>
