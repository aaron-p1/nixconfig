export config = ->
  import "helper"

  vim.keymap.set("n", "<Leader>gbb", "<Cmd>Git blame<CR>", {silent: true, desc: "Whole file"})

  helper.registerPluginWk({
    map: {
      g: {
        name: "Git",
        b: {
          name: "Blame",
        },
      },
    },
  })
