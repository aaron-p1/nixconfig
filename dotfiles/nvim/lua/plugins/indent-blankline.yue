export config = ->
  import "indent_blankline" as ib

  ib.setup({
    filetype_exclude:
      * "help"
      * "packer"
    -- for example, context is off by default, use this to turn it on
    use_treesitter: true
    show_current_context: true
    show_current_context_start: true
  })
