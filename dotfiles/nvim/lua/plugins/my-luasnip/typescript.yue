import "fun" as {
  :op
}

import "helper" as {
  :chain
}

import "plugins.my-luasnip.helper" as {
  :s
  :i
  :fmta
  :conds
}

export default {
  s("ed", fmta("export default <>;", i(0)))
  s("fn", fmta("(<>) =>> {\n\t<>\n}", {i(1), i(0)}), {
    condition: chain(conds.line_begin, op.lnot),
  })
}
