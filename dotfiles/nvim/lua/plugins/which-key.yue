export config = ->
  import "which-key" as wk
  import "helper"

  wk.setup({})

  helper.registerPluginWk({
    prefix: "<leader>"
    map:
      t:
        name: "Tab"
      d:
        name: "Diff"
      c:
        name: "Compare"
  })
