export config = ->
  import "nvim-treesitter.configs" as tsc

  tsc.setup({
    highlight:
      enable: true -- false will disable the whole extension
    indent:
      enable: true
    autotag:
      enable: true
      filetypes:
        * "html"
        * "xml"
        * "blade"
        * "vue"
  })
